export enum language {
  UZ = 'uz',
  RU = 'ru',
  EN = 'en',
}

export enum UserRoleEnum {
  SUPER_ADMIN = 'super_admin',
  ADMIN = 'admin',
  CLIENT = 'client',
  OPERATOR = 'operator',
}

export enum BalanceOperationEnum {
  ADD = 'add',
  SUBTRACT = 'subtract',
  SET = 'set',
}
