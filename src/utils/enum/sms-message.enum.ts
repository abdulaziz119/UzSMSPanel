export enum MessageStatusEnum {
  PENDING = 'pending',
  SENT = 'sent',
  DELIVERED = 'delivered',
  FAILED = 'failed',
  REJECTED = 'rejected',
}

export enum MessageDirectionEnum {
  OUTBOUND = 'outbound',
  INBOUND = 'inbound',
}
